# 🎓 WORK21 Admin Panel

Панель администратора для платформы WORK21 — управление пользователями, проектами, заявками и договорами.

## 🚀 Технологии

- **React 18** — UI библиотека
- **Refine** — фреймворк для админ-панелей
- **Ant Design** — UI компоненты
- **TypeScript** — типизация
- **Vite** — сборщик
- **Recharts** — графики
- **Axios** — HTTP клиент

## 📦 Установка

### Локальная разработка

```bash
# Клонирование
git clone https://github.com/ChargeOnTop/work21-admin.git
cd work21-admin

# Установка зависимостей
npm install

# Настройка окружения
cp .env.example .env
# Отредактируйте .env файл

# Запуск dev-сервера
npm run dev
```

Админка будет доступна по адресу: http://localhost:3000

### Docker

```bash
# Сборка образа
docker build -t work21-admin \
  --build-arg VITE_API_URL=http://your-api-url/api/v1 .

# Запуск контейнера
docker run -p 3000:80 work21-admin

# Или с docker-compose
docker-compose up -d
```

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_URL` | URL бэкенда API | `http://localhost:8000/api/v1` |
| `VITE_APP_TITLE` | Название приложения | `WORK21 Admin` |

## 📁 Структура проекта

```
work21-admin/
├── src/
│   ├── pages/              # Страницы
│   │   ├── dashboard/      # Дашборд со статистикой
│   │   ├── users/          # Управление пользователями
│   │   ├── projects/       # Управление проектами
│   │   ├── applications/   # Заявки студентов
│   │   ├── contracts/      # Договоры
│   │   ├── ratings/        # Рейтинги
│   │   └── login/          # Страница входа
│   │
│   ├── providers/          # Провайдеры Refine
│   │   ├── authProvider.ts # Аутентификация
│   │   └── dataProvider.ts # Работа с API
│   │
│   ├── types/              # TypeScript типы
│   ├── utils/              # Утилиты и константы
│   ├── App.tsx             # Главный компонент
│   └── main.tsx            # Точка входа
│
├── public/                 # Статические файлы
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose
├── nginx.conf              # Конфигурация Nginx
└── package.json
```

## 🔐 Функционал

### Пользователи
- ✅ Просмотр списка пользователей с фильтрацией
- ✅ Поиск по email, имени
- ✅ Фильтрация по роли и статусу
- ✅ Просмотр детальной информации
- ✅ Редактирование данных пользователя
- ✅ **Сброс пароля**
- ✅ **Блокировка/разблокировка**

### Проекты
- ✅ Список проектов с фильтрацией по статусу
- ✅ Просмотр деталей проекта
- ✅ Редактирование проекта
- ✅ Просмотр AI-оценки и ТЗ

### Заявки
- ✅ Список заявок студентов
- ✅ Фильтрация по статусу
- ✅ Принятие/отклонение заявок

### Договоры
- ✅ Список договоров
- ✅ Фильтрация по статусу

### Рейтинги
- ✅ Просмотр всех оценок
- ✅ Удаление рейтингов

### Дашборд
- ✅ Общая статистика платформы
- ✅ Графики распределения пользователей
- ✅ Статусы проектов
- ✅ Последние проекты и пользователи

## 🔌 Требования к Backend API

Админка ожидает следующие endpoints:

```
POST   /api/v1/auth/login              # Авторизация
GET    /api/v1/admin/stats             # Статистика
GET    /api/v1/admin/users             # Список пользователей
GET    /api/v1/admin/users/:id         # Детали пользователя
PATCH  /api/v1/admin/users/:id         # Обновление пользователя
POST   /api/v1/admin/users/:id/reset-password  # Сброс пароля
POST   /api/v1/admin/users/:id/block   # Блокировка
POST   /api/v1/admin/users/:id/unblock # Разблокировка
GET    /api/v1/admin/projects          # Список проектов
GET    /api/v1/admin/projects/:id      # Детали проекта
PATCH  /api/v1/admin/projects/:id      # Обновление проекта
DELETE /api/v1/admin/projects/:id      # Удаление проекта
GET    /api/v1/admin/applications      # Список заявок
PATCH  /api/v1/admin/applications/:id  # Обновление заявки
GET    /api/v1/admin/contracts         # Список договоров
GET    /api/v1/admin/ratings           # Список рейтингов
DELETE /api/v1/admin/ratings/:id       # Удаление рейтинга
```

## 🛠️ Скрипты

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для production
npm run preview  # Предпросмотр сборки
npm run lint     # Проверка кода
```

## 📋 Интеграция с work21-deploy

Добавьте в `docker-compose.yml` проекта work21-deploy:

```yaml
services:
  # ... другие сервисы ...
  
  admin:
    build: ./admin
    environment:
      - VITE_API_URL=http://backend:8000/api/v1
    depends_on:
      - backend
    restart: unless-stopped
```

И обновите nginx конфигурацию:

```nginx
location /admin/ {
    proxy_pass http://admin:80/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
```

## 🔒 Безопасность

- Только пользователи с ролью `admin` могут войти
- JWT токены хранятся в localStorage
- Автоматический logout при 401 ошибке
- HTTPS рекомендуется для production

## 📄 Лицензия

MIT

## 🤝 Связанные репозитории

- [work21-backend](https://github.com/ChargeOnTop/work21-backend) — Backend API
- [work21-agent](https://github.com/ChargeOnTop/work21-agent) — AI Agent
- [work21-fr](https://github.com/ChargeOnTop/work21-fr) — Frontend
- [work21-deploy](https://github.com/ChargeOnTop/work21-deploy) — Deployment

